"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractCloudinaryError = exports.buildSearchExpression = exports.createMultipartBody = exports.generateCloudinarySignature = exports.metadataToPipeString = exports.splitCsvIds = exports.buildResourceDeleteUrl = exports.buildTagsActionUrl = exports.joinTransformation = exports.buildDeliveryUrl = exports.buildResourceByAssetIdUrl = exports.buildResourceUpdateUrl = exports.buildUploadUrl = exports.basicAuth = exports.jsonHeaders = exports.USER_AGENT = void 0;
const n8n_workflow_1 = require("n8n-workflow");
const sha256_utils_1 = require("./sha256.utils");
const package_json_1 = require("../../package.json");
const CLOUDINARY_API_BASE = 'https://api.cloudinary.com/v1_1';
exports.USER_AGENT = `n8n-nodes-cloudinary/${package_json_1.version}`;
const jsonHeaders = () => ({
    'Content-Type': 'application/json',
    'User-Agent': exports.USER_AGENT,
});
exports.jsonHeaders = jsonHeaders;
const basicAuth = (creds) => ({
    username: creds.apiKey,
    password: creds.apiSecret,
});
exports.basicAuth = basicAuth;
const buildUploadUrl = (cloudName, resourceType) => `${CLOUDINARY_API_BASE}/${cloudName}/${resourceType}/upload`;
exports.buildUploadUrl = buildUploadUrl;
const buildResourceUpdateUrl = (cloudName, resourceType, type, publicId) => `${CLOUDINARY_API_BASE}/${cloudName}/resources/${resourceType}/${type}/${publicId}`;
exports.buildResourceUpdateUrl = buildResourceUpdateUrl;
const buildResourceByAssetIdUrl = (cloudName, assetId) => `${CLOUDINARY_API_BASE}/${cloudName}/resources/${assetId}`;
exports.buildResourceByAssetIdUrl = buildResourceByAssetIdUrl;
const SHARED_DELIVERY_HOST = 'res.cloudinary.com';
const normalizeDeliveryHost = (host) => host
    .trim()
    .replace(/^https?:\/\//i, '')
    .replace(/\/+$/, '');
const smartEscapeFetchUrl = (source) => source.replace(/[^A-Za-z0-9_.\-/:]+/g, (run) => encodeURIComponent(run));
const buildDeliveryUrl = (opts) => {
    const { cloudName, resourceType, type, publicId, transformation, format, version, privateCdn, secureDistribution, } = opts;
    const cname = secureDistribution ? normalizeDeliveryHost(secureDistribution) : '';
    const host = cname || (privateCdn ? `${cloudName}-${SHARED_DELIVERY_HOST}` : SHARED_DELIVERY_HOST);
    const includeCloudName = !privateCdn && !cname;
    const id = type === 'fetch' ? smartEscapeFetchUrl(publicId) : publicId;
    const idWithFormat = format ? `${id}.${format}` : id;
    const versionSegment = version !== undefined && version !== '' ? `v${version}` : undefined;
    const segments = [
        includeCloudName ? cloudName : undefined,
        resourceType,
        type,
        transformation,
        versionSegment,
        idWithFormat,
    ].filter((s) => s !== undefined && s !== '');
    return `https://${host}/${segments.join('/')}`;
};
exports.buildDeliveryUrl = buildDeliveryUrl;
const joinTransformation = (components) => components
    .map((c) => (c == null ? '' : c.trim()))
    .filter((c) => c.length > 0)
    .join('/');
exports.joinTransformation = joinTransformation;
const buildTagsActionUrl = (cloudName, resourceType) => `${CLOUDINARY_API_BASE}/${cloudName}/${resourceType}/tags`;
exports.buildTagsActionUrl = buildTagsActionUrl;
const buildResourceDeleteUrl = (cloudName, resourceType, type) => `${CLOUDINARY_API_BASE}/${cloudName}/resources/${resourceType}/${type}`;
exports.buildResourceDeleteUrl = buildResourceDeleteUrl;
const splitCsvIds = (csv) => csv
    .split(',')
    .map((s) => s.trim())
    .filter((s) => s.length > 0);
exports.splitCsvIds = splitCsvIds;
const escapeMetadataValue = (value) => value.replace(/([=|"])/g, '\\$1');
const metadataToPipeString = (input) => {
    let metadata;
    try {
        metadata = typeof input === 'object' ? input : JSON.parse(input);
    }
    catch (error) {
        throw new n8n_workflow_1.ApplicationError('Invalid JSON for structured metadata');
    }
    return Object.keys(metadata)
        .map((key) => {
        const value = metadata[key];
        if (value === undefined || value === null) {
            return undefined;
        }
        if (Array.isArray(value)) {
            const items = value
                .map((item) => `"${escapeMetadataValue(String(item))}"`)
                .join(',');
            return `${key}=[${items}]`;
        }
        return `${key}=${escapeMetadataValue(String(value))}`;
    })
        .filter((pair) => pair !== undefined)
        .join('|');
};
exports.metadataToPipeString = metadataToPipeString;
const generateCloudinarySignature = (params, apiSecret) => {
    const { signature, api_key, file, resource_type, cloud_name, ...paramsToSign } = params;
    const sortedParams = Object.keys(paramsToSign)
        .filter((key) => {
        const value = paramsToSign[key];
        return value !== undefined && value !== null && value !== '';
    })
        .sort()
        .map((key) => `${key}=${paramsToSign[key]}`)
        .join('&');
    const stringToSign = `${sortedParams}${apiSecret}`;
    return (0, sha256_utils_1.sha256)(stringToSign);
};
exports.generateCloudinarySignature = generateCloudinarySignature;
const createMultipartBody = (fields, fileData, fileName, mimeType) => {
    const boundary = `----formdata-n8n-${Math.random().toString(16).slice(2)}`;
    const CRLF = '\r\n';
    let body = '';
    for (const [name, value] of Object.entries(fields)) {
        body += `--${boundary}${CRLF}`;
        body += `Content-Disposition: form-data; name="${name}"${CRLF}`;
        body += CRLF;
        body += value;
        body += CRLF;
    }
    const safeFileName = fileName.replace(/[\r\n]/g, '').replace(/"/g, "'");
    body += `--${boundary}${CRLF}`;
    body += `Content-Disposition: form-data; name="file"; filename="${safeFileName}"${CRLF}`;
    body += `Content-Type: ${mimeType}${CRLF}`;
    body += CRLF;
    const textBuffer = Buffer.from(body, 'utf8');
    const endBuffer = Buffer.from(`${CRLF}--${boundary}--${CRLF}`, 'utf8');
    const finalBody = Buffer.concat([textBuffer, fileData, endBuffer]);
    return { body: finalBody, boundary };
};
exports.createMultipartBody = createMultipartBody;
const buildSearchExpression = (input, resourceTypes) => {
    const hasResourceTypeClause = /\bresource_type\s*[:=]/i.test(input);
    if (hasResourceTypeClause || resourceTypes.length === 0 || resourceTypes.length >= 3) {
        return input;
    }
    const clause = resourceTypes.length === 1
        ? `resource_type:${resourceTypes[0]}`
        : `resource_type:(${resourceTypes.join(' OR ')})`;
    return input ? `(${input}) AND ${clause}` : clause;
};
exports.buildSearchExpression = buildSearchExpression;
const extractCloudinaryError = (error) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r;
    const status = ((_b = (_a = error === null || error === void 0 ? void 0 : error.httpCode) !== null && _a !== void 0 ? _a : error === null || error === void 0 ? void 0 : error.statusCode) !== null && _b !== void 0 ? _b : (_c = error === null || error === void 0 ? void 0 : error.response) === null || _c === void 0 ? void 0 : _c.status);
    const headers = ((_e = (_d = error === null || error === void 0 ? void 0 : error.response) === null || _d === void 0 ? void 0 : _d.headers) !== null && _e !== void 0 ? _e : (_g = (_f = error === null || error === void 0 ? void 0 : error.cause) === null || _f === void 0 ? void 0 : _f.response) === null || _g === void 0 ? void 0 : _g.headers);
    const retryAfter = (_h = headers === null || headers === void 0 ? void 0 : headers['retry-after']) !== null && _h !== void 0 ? _h : headers === null || headers === void 0 ? void 0 : headers['Retry-After'];
    const cloudinaryMessage = (_r = (_m = (_l = (_k = (_j = error === null || error === void 0 ? void 0 : error.response) === null || _j === void 0 ? void 0 : _j.body) === null || _k === void 0 ? void 0 : _k.error) === null || _l === void 0 ? void 0 : _l.message) !== null && _m !== void 0 ? _m : (_q = (_p = (_o = error === null || error === void 0 ? void 0 : error.response) === null || _o === void 0 ? void 0 : _o.data) === null || _p === void 0 ? void 0 : _p.error) === null || _q === void 0 ? void 0 : _q.message) !== null && _r !== void 0 ? _r : headers === null || headers === void 0 ? void 0 : headers['x-cld-error'];
    return { status, retryAfter, cloudinaryMessage };
};
exports.extractCloudinaryError = extractCloudinaryError;
//# sourceMappingURL=cloudinary.utils.js.map